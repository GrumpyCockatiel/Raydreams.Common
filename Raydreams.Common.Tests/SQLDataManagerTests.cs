using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Raydreams.Common.Logging;

namespace Raydreams.Common.Tests
{
    [TestClass]
    public class SQLDataManagerTests
    {
        [TestMethod()]
        public void SimpleQueryTest()
        {
            //SqlLogger repo = new SqlLogger( "Test", ConnectionString );
            //var results = repo.GetTop();

            //Assert.IsTrue(results.Count > 0);
        }

    }
}
